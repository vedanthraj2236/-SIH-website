import { Search as SearchIcon, SlidersHorizontal } from 'lucide-react'
import { useMemo, useState } from 'react'
import { Link, useSearchParams } from 'react-router-dom'
import StateCard from '../components/StateCard'
import { INDIA_REGIONS, slugify, stateData } from '../data/states'

export default function Search(){const [params]=useSearchParams(); const initial=params.get('q')||''; const [q,setQ]=useState(initial); const [onlyReady,setOnlyReady]=useState(false); const results=useMemo(()=>INDIA_REGIONS.filter(name=>{const match=name.toLowerCase().includes(q.toLowerCase().trim()); const ready=!!stateData[slugify(name)]; return match&&(!onlyReady||ready)}),[q,onlyReady]); return <main className="content-page mx-auto max-w-7xl px-4 py-12 md:px-8 md:py-16"><div className="max-w-3xl"><div className="text-xs font-bold uppercase tracking-[.2em] text-maroon/70">Search the atlas</div><h1 className="mt-2 font-display text-5xl">Find a place to begin.</h1><p className="mt-4 text-sm leading-6 text-ink/55">Search every state and union territory. Ready profiles are surfaced with their cultural highlights.</p></div><div className="mt-8 flex flex-col gap-3 md:flex-row md:items-center"><div className="flex flex-1 items-center gap-3 rounded-full border border-maroon/15 bg-white px-5 py-4 shadow-card"><SearchIcon size={18} className="text-maroon"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Try Karnataka, Kerala, Assam…" className="w-full bg-transparent text-sm outline-none"/></div><label className="inline-flex cursor-pointer items-center gap-2 rounded-full border border-maroon/10 bg-white px-4 py-3 text-sm font-semibold shadow-card"><input type="checkbox" checked={onlyReady} onChange={e=>setOnlyReady(e.target.checked)} className="accent-maroon"/><SlidersHorizontal size={15}/> Ready profiles only</label></div><div className="mt-7 flex items-center justify-between text-sm text-ink/45"><span>{results.length} result{results.length===1?'':'s'}</span>{q&&<Link to="/search" className="text-maroon font-semibold">Clear</Link>}</div><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{results.map(name=><StateCard key={name} name={name} data={stateData[slugify(name)]}/>)}</div></main>}
