import { ArrowDownUp, GitCompare } from 'lucide-react'
import { useState } from 'react'
import CompareView from '../components/CompareView'
import { INDIA_REGIONS, slugify, stateData } from '../data/states'

function Select({value,onChange,label,exclude}){return <label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[.16em] text-maroon/65">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-2xl border border-maroon/15 bg-white px-4 py-3 text-sm font-semibold outline-none focus:border-maroon focus:ring-2 focus:ring-saffron/20"><option value="">Choose a state…</option>{INDIA_REGIONS.filter(n=>n!==exclude).map(n=><option key={n} value={slugify(n)}>{n}{!stateData[slugify(n)]?' · coming soon':''}</option>)}</select></label>}
export default function Compare(){const [a,setA]=useState('karnataka');const [b,setB]=useState('kerala'); return <main className="content-page mx-auto max-w-7xl px-4 py-12 md:px-8 md:py-16"><div className="max-w-3xl"><div className="text-xs font-bold uppercase tracking-[.2em] text-maroon/70">Cultural compare</div><h1 className="mt-2 font-display text-5xl">Put two regions side by side.</h1><p className="mt-4 text-sm leading-6 text-ink/55">Same schema, same fields, fewer assumptions. This is useful for spotting similarities and differences before adding new profiles.</p></div><div className="mt-8 grid gap-4 rounded-[2rem] border border-maroon/10 bg-white p-5 shadow-card md:grid-cols-[1fr_auto_1fr] md:items-end md:p-7"><Select label="Region A" value={a} onChange={setA} exclude={b ? Object.keys(Object.fromEntries(INDIA_REGIONS.map(n=>[slugify(n),n]))).find(s=>s===b):''}/><div className="mx-auto grid h-11 w-11 place-items-center rounded-full bg-maroon text-gold"><ArrowDownUp size={17}/></div><Select label="Region B" value={b} onChange={setB} exclude={a ? Object.keys(Object.fromEntries(INDIA_REGIONS.map(n=>[slugify(n),n]))).find(s=>s===a):''}/></div><div className="mt-8"><CompareView left={stateData[a]} right={stateData[b]} leftName={stateData[a]?.state || 'Select a ready profile'} rightName={stateData[b]?.state || 'Select a ready profile'}/></div></main>}
